
<p><strong>Title: </strong>
  <%= @book.title %></p>

<p><strong>Body: </strong>
  <%= @book.body %></p>

<% if user_signed_in? && current_user.has_posted?(@book)%>
  <%= link_to 'Edit', edit_book_path(@book) %>
<%end%>
|
<%= link_to 'Back', books_path %>
